{
  "name": "voxel-sandbox",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && vite build && node scripts/inline-build.mjs",
    "check": "npm run build"
  },
  "dependencies": {
    "three": "^0.164.1"
  },
  "devDependencies": {
    "@types/three": "^0.164.0",
    "typescript": "^5.4.5",
    "vite": "^5.2.11"
  }
}
